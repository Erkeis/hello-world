# Knowledge Asset: Session Insights (2026-04-15)

## 🔑 Core Keywords
`NotebookLM RPC-Link` `Lean Architecture` `Metadata-Driven Routing` `PII Anonymization` `Junction Skill-Linking` `Show-Don't-Tell Prompting` `Negative Triggers`

---

## 🏛️ 1. Architecture: Lean Context Engineering
- **Show, Don't Tell:** LLMs focus naturally on structured data (YAML/Headers). Verbose meta-instructions like "Save tokens by reading only X" are themselves token-wasteful.
- **Centralized Routing Table:** Instead of distributed triggers, a single `routing-tools.md` table acts as a "Cognitive Map," allowing agents to decide which fragment to load in one turn.
- **Strict Typing & Prefixing:** Using filename prefixes (`ref_`, `guide_`) combined with a strict `name`/`description` frontmatter schema creates a high-speed discovery phase.

## 🛠️ 2. Tooling: NotebookLM 2026 Ecosystem
- **NLM-MCP-CLI (Jacob-BD):** Preferred over browser automation. Stable, RPC-based, and supports 35+ tools including Audio/Video Overview generation.
- **Persistent Memory Layer:** NotebookLM is no longer a side-tool; it is the persistent long-term memory for Gemini CLI, accessible via the `nlm` command or web-UI sidebar.

## 🛡️ 3. Security: PII Protection Workflow
- **Placeholder Strategy:** Always use `<USER_HOME>`, `<USER_EMAIL>`, and `<PROJECT_ROOT>` when documenting local environment setups to ensure session logs remain public-safe.
- **Cloud-Source Replacement:** When PII is detected in NotebookLM sources, the correct workflow is: 
  `Local Anonymize` ➔ `Cloud Delete (with --confirm)` ➔ `Cloud Add (with --wait)`.

## ⚡ 4. Reusable Automation Snippets
- **Skill Linking (Junction):** Use Windows Junctions to sync global `.gemini/skills` with local `.agent/skills` for real-time updates.
- **Multiline String Workaround:** To avoid PowerShell parser errors in CLI tools, use the "Write-to-Temp-then-Move" pattern instead of direct multiline injection.

---
*Generated by Gemini CLI & Antigravity Assistant.*
